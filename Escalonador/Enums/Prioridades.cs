namespace Escalonador.Enums
{
    /// <summary>
    /// Enum que representa as prioridades da Tarefa.
    /// </summary>
    public enum Prioridades
    {
        Alta = 1,
        Media = 2,
        Baixa = 3,
    }
}
